apiVersion: v1
kind: Service
metadata:
  name: {{ include "pr-reviewer.fullname" . }}
  labels:
    {{- include "pr-reviewer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "pr-reviewer.selectorLabels" . | nindent 4 }}
